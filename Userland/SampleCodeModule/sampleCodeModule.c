#include <stringLib.h>
#include <shell.h>

/* sampleCodeModule.c */

int main() {

while (1){
      char c = getchar();
      if (c != 0) {
            putchar(c);
      }
 }
//	runShell();
	
return 0;
}